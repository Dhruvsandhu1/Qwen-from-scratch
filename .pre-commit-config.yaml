repos:
  - repo: https://github.com/psf/black
    rev: 24.1.0
    hooks:
      - id: black
        language_version: python3.11


  # mypy is temporarily disabled in pre-commit because the local model code
  # contains typed areas that need refactoring. Enable once types are fixed.
