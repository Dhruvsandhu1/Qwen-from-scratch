"""
## Add a new feature to handle user inputs

To handle user inputs, we need to create a new function called `handleUserInput` that will process the input and return the appropriate response.

We can use the following steps:
1. Create a function called `handleUserInput` in the `user_inputs` directory.
2. Add the necessary code to handle the user inputs.
3. Test the function to ensure it works correctly.

Once the function is created, we can test it by running the following command: `pytest tests`

We will need to run this command to verify that the function works correctly.
"""


def handleUserInput(input_data):
    pass
