# Runtime dependencies (pinned for reproducible inference)
transformers
accelerate
# PyTorch: install the correct build for your CUDA version. Minimum required:
# For CPU-only: pip install torch --index-url https://download.pytorch.org/whl/cpu
# For CUDA: follow https://pytorch.org/get-started/locally/
torch
safetensors
tokenizers