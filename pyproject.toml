[tool.black]
line-length = 3000
target-version = ['py311']

[tool.isort]
profile = "black"

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.pyright]
extraPaths = [".", "/"]
